import {Router} from 'express';

const router = Router();

router.post('/', (req, res) => {})

router.get('/', (req, res) => {})

router.patch('/:id', (req, res) => {})

router.delete('/:id', (req, res) => {})

export default router;